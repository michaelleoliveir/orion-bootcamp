version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    env_file:
      - .env
    volumes:
      - .:/app
    command: node exercicio-04.js
    networks:
      - database_orion_network

networks:
  database_orion_network:
    external: true
